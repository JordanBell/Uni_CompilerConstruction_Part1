doub (x)
{
	x * x
}

main (x, y, z)
{
	// Evaluate the total of the arguments and print & return its value, doubled
	int x = 0;
	int y = 0;
	int z = 0;
	int n = x * y * z;
	n = doub({n});
	print_int (n);
	~n
}
